task hello{
	doLast{					//最后一个执行
		println"Hello World!"
		logger.quiet("debug日志")
	}
}

task strTask{
	def str1="双引号"
	logger.quiet("第二个task${str1}")
	logger.quiet('第二个task${str1}')

}
// 集合
task ListTask{
	def numList=[1,2,3,4,9]
	println numList[1]//访问第二个元素
	println numList[-1]//访问最后一个元素
	println numList[1..3]//2到4个元素
	println numList[-3..-1]//2到4个元素
	numList.each { 
		println it
		
	}
}
//Map 
task mapTask{
		def mapList=['weight':1024,'height':1980]
		println mapList['weight']
		println mapList.weight

		mapList.each { 
			println "Key:${it.key},Value:${it.value}"
			
		}
}



task inovkeMethod{
	method(1,2)
	method 1,2

}
def method (int a,int b){
	println a+b
	println "方法返回${methodReturn a,b}"
}

def methodReturn(int a,int b){
	if (a>b) {
		a
		
	}else{
		b
	}
}