task hello{
	doLast{					//最后一个执行
		println"Hello World!"
		logger.quiet("debug日志")
	}
}

task strTask{
	def str1="双引号"
	logger.quiet("第二个task${str1}")
	logger.quiet('第二个task${str1}')

}
// 集合
task ListTask{
	def numList=[1,2,3,4,9]
	println numList[1]//访问第二个元素
	println numList[-1]//访问最后一个元素
	println numList[1..3]//2到4个元素
	println numList[-3..-1]//2到4个元素
	numList.each { 
		println it
		
	}
}
//Map 
task mapTask{
		def mapList=['weight':1024,'height':1980]
		println mapList['weight']
		println mapList.weight

		mapList.each { 
			println "Key:${it.key},Value:${it.value}"
			
		}
}



task inovkeMethod{
	method(1,2)
	method 1,2

}
def method (int a,int b){
	println a+b
	println "方法返回${methodReturn a,b}"
}

def methodReturn(int a,int b){
	if (a>b) {
		a
	}else{
		b
	}
}


//JaveBean
class Person{
	private String name

	public int getAge(){
		12
	}
	public setAge(int a){
		a
	}

}
task JaveBeanClass{
	Person p=new Person()
	p.name="张三"
	println p.name
	println p.age
	p.setAge(15)
	println p.age
}

task helloClosure{
	eachMap{k,v ->
		println "${k} is ${v}"
	}
}
def eachMap (clossure){
	def map=["name":"张三","age":18]
	map.each{
		clossure(it.key,it.value)
}


DSL (Domain Specific Language) 领域特定语言
